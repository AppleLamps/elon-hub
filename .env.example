XAI_API_KEY=your_xai_api_key_here

# Neon PostgreSQL Database (keep secure, do not expose to client-side code)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Optional: Cron secret for securing scheduled jobs (recommended for production)
CRON_SECRET=your_random_secret_here
